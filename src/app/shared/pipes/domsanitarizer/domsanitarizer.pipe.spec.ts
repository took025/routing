import { DomsanitarizerPipe } from './domsanitarizer.pipe';

describe('DomsanitarizerPipe', () => {
  it('create an instance', () => {
    const pipe = new DomsanitarizerPipe();
    expect(pipe).toBeTruthy();
  });
});
